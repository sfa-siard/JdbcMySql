# This workflow will build a Java project with Ant
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-ant

name: Test and build with ant

on:
  push:
    branches:
      - master
      - dev/pitc-*
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: sudo chown -R $USER:$USER ${{ github.workspace }}
      - uses: actions/checkout@v2
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
      - name: run docker-compose
        uses: sudo-bot/action-docker-compose@latest
        with:
          # https://docs.docker.com/compose/reference/overview/
          cli-args: "up -d"
      # TODO: find a better way to wait until the database is ready
      - name: Sleep for 20 seconds
        run: sleep 20s
        shell: bash
      - name: check logs of docker-compose
        uses: sudo-bot/action-docker-compose@latest
        with:
          # https://docs.docker.com/compose/reference/overview/
          cli-args: "logs"
      - name: Verify MySQL connection from host with host 127.0.0.1
        continue-on-error: true
        run: |
          sudo apt-get install -y mysql-client-8.0 mysql-client
          mysql --host 127.0.0.1 --port 3306 -uroot -prootpwd -e "SHOW DATABASES"
      - name: Verify MySQL connection from host with host 127.0.0.1 without installation
        continue-on-error: true
        run: mysql --host 127.0.0.1 --port 3306 -uroot -prootpwd -e "SHOW DATABASES"
      - name: Verify MySQL connection from host with host localhost
        continue-on-error: true
        run: |
          sudo apt-get install -y mysql-client-8.0 mysql-client
          mysql --host localhost --port 3306 -uroot -pprootpwd -e "SHOW DATABASES"
      - name: Build with Ant
        run: ant -noinput -buildfile build.xml build
      - name: Tests with Ant
        run: ant -noinput -buildfile build.xml test
      - run: ls -la ./tmp
      - name: Upload datasource test output file
        uses: actions/upload-artifact@v2
        with:
          name: datasource-test.txt
          path: ${{ github.workspace}}/tmp/datasource-test.txt
      - name: Upload test output file
        uses: actions/upload-artifact@v2
        with:
          name: jdbc-tests.txt
          path: ${{ github.workspace }}/tmp/jdbc-tests.txt
